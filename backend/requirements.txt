Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
python-dotenv==1.0.0

# Production server + static files
gunicorn==21.2.0
whitenoise==6.6.0

# Postgres (Railway managed DB)
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Image handling (Django ImageField uses this for User.avatar)
Pillow==10.2.0

# Rate limiting for auth endpoints (Phase 10)
django-ratelimit==4.1.0
