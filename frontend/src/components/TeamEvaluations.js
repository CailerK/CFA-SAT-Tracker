import React from 'react';
import Leadership360Evaluations from './Leadership360Evaluations';

const TeamEvaluations = ({ user, onNavigate }) => (
  <Leadership360Evaluations user={user} onNavigate={onNavigate} />
);

export default TeamEvaluations;
